import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#302353",
      light: "#805CDD",
    },
    secondary: {
      main: "#03ff00",
    },
    text: {
      primary: "#FFFFFF",
      secondary: "#E6E6E6",
    },
  },
});

export default theme;
